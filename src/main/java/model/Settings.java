package model;

public class Settings {

    int width;
    int height;
    int playerCount;

    public Settings(int width, int height, int playerCount) {
        this.width = width;
        this.height = height;
        this.playerCount = playerCount;
    }
}
